# 领域词汇

## ADB 候选（ADB candidate）

应用内置、发现或由用户选择的 ADB 可执行文件。候选只有在通过版本验证后才有效，并记录来源、路径和版本。

## 内置 ADB（bundled ADB）

随应用发布并由应用拥有的 ADB 候选。没有已保存的用户选择时，它是默认的当前 ADB；用户仍可切换到外部 ADB。

## 当前 ADB（selected ADB）

当前被应用用于管理服务器和设备的唯一 ADB 候选。一次设备会话中的列举、连接、截图和点击都使用它。

## 设备（device）

当前 ADB 可见的 Android 目标。设备具有序列号、可选型号、传输标识和在线、离线、未授权或未知状态。

## 活动设备（active device）

用户当前操作的唯一在线设备。只有活动设备能产生截图或接收点击；切换活动设备会使旧截图失效。

## 截图帧（screenshot frame）

活动设备在某一时刻产生的 PNG 画面，以及它的宽度、高度、设备归属和获取时间。截图帧是验证后续坐标点击的依据。

## 选中坐标（selected point）

用户在当前截图帧上选择或用键盘输入的整数像素位置。选中坐标本身不会操作设备。

## 设备点击（tap）

用户明确确认后，在当前活动设备与当前截图边界内执行的一次坐标操作。成功结果包含设备、坐标和完成时间。

## 点击目标（click target）

当前截图帧上的一个精确点或轴对齐矩形点击区域。点击区域只属于当前帧，不持久化；最终落点由 `DeviceManager` 在合法整数像素中采样。

## 最终落点（final point）

随机延时结束且设备与截图复核通过后，实际发送给 ADB 的像素坐标。存在其他合法候选时，它不同于同设备上一次最终落点；单点目标也不同于其中心点。

## 活动配置（activity configuration）

用户在本机校准的版本化配置，绑定画面尺寸和方向，包含页面状态的不可还原视觉特征、安全动作区域、已知弹窗规则和点击时序；不包含截图或裁剪像素。

## 识别状态（recognized state）

当前截图中唯一达到阈值并以足够差值领先其他候选的页面状态。低置信度和歧义都不是识别状态，不会产生设备输入。

## 任务会话（activity session）

由 `DeviceManager` 独占的单活动设备运行状态，记录目标与完成次数、重试、暂停原因和最近安全动作。应用重启时始终回到空闲。

## 已知弹窗（known popup）

用户在本机配置了视觉特征与关闭区域的弹窗。只有唯一匹配且未超过当前转换的处理上限时才允许关闭；其他弹窗一律暂停。

## 成功结算（successful settlement）

识别到奖励页、执行领取动作并再次唯一识别返回挑战页的完整结果。返回挑战页是领取奖励后回到挑战入口的独立页面状态；只有此时任务完成次数增加。

## 实时预览（live preview）

活动设备产生的低延迟连续画面。预览会话由 `DeviceManager` 独占管理；停止、设备切换或设备失效会终止会话并使选中坐标失效。当前 `screenrecord` + WebCodecs 路径仅为 Windows 可行性原型，不代表正式支持承诺。
